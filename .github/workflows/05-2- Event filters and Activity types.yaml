name: 05 - 2 filters and activity types

on:
    workflow_dispatch:
        types: [closed]
        branches: 
          - main


jobs: 
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: echo "Running whenever a PR is closed"
         